import { TotalTtcClientPipe } from './total-ttc-client.pipe';

describe('TotalTtcClientPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalTtcClientPipe();
    expect(pipe).toBeTruthy();
  });
});
